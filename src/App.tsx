
export const App = ()=>{

    return   (<div className="container">
        <>
        <h1>Mac OS Theme switcher</h1>
        <button onClick={()=>{

        }}>Light mode</button>
        <button>Dark mode</button>
        </>
    </div>)
}